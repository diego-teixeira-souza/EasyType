#ifndef CONST_VOLATILE_SIGNATURE_IMPLEMENTATION_3_HPP
#define CONST_VOLATILE_SIGNATURE_IMPLEMENTATION_3_HPP

namespace easytype
{

namespace qualifier
{

namespace constvolatile
{

namespace signature
{

// GENERATE(T***);
// GENERATE(T**const*);
// GENERATE(T**volatile*);
// GENERATE(T**const volatile*);

// GENERATE(T*const**);
// GENERATE(T*const*const*);
// GENERATE(T*const*volatile*);
// GENERATE(T*const*const volatile*);

// GENERATE(T*volatile**);
// GENERATE(T*volatile*const*);
// GENERATE(T*volatile*volatile*);
// GENERATE(T*volatile*const volatile*);

// GENERATE(T*const volatile**);
// GENERATE(T*const volatile*const*);
// GENERATE(T*const volatile*volatile*);
// GENERATE(T*const volatile*const volatile*);


}	
	
}	
	
}	
	
}

#endif